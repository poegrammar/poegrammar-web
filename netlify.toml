[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.20.8"

[build.cache]
  paths = ["node_modules"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
